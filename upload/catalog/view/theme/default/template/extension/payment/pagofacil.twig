<form class="form-horizontal" action="{{ url }}" method="post" id="payment">
  <div class="buttons">
    <div class="pull-right">
      <button type="submit" class="btn btn-primary" id="button_confirm">{{ button_confirm }}</button>
    </div>
  </div>
</form>
